version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      RELEASE: DEV
    volumes:
      - ./models:/usr/app/models
